USE_BOOTLOADER := false

F0_TARGETS  := $(TARGET)

FLASH_SIZE  := 32
STM_CHIP    := STM32F031x6
TARGET_SRC  := \

ifneq ($(USE_BOOTLOADER),true)
TARGET_FLAGS := -D $(STM_CHIP)
else
TARGET_FLAGS := -D $(STM_CHIP) -D USE_BOOTLOADER
endif
